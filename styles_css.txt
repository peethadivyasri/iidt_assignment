body {
    font-family: Arial, sans-serif;
    margin: 0;
    height: 100vh;
    display: flex;
    justify-content: center;
    align-items: center;
    background-color: #f0f0f0;
}

.color-box {
    text-align: center;
    position: relative;
}

.color-info {
    text-align: center;
    margin-bottom: 20px;
    position: absolute;
    bottom: calc(100% + 20px); /* Position just above the click box */
    left: 50%;
    transform: translateX(-50%);
    white-space: nowrap; /* Prevent wrapping */
    font-size: 18px; /* Set font size */
}

.color-sample {
    padding: 10px;
    color: white;
    background-color: black;
    border-radius: 5px;
    display: inline-block; /* Display as inline block */
}

#color-code {
    font-size: 18px; /* Set font size */
}

.click-box {
    position: relative;
}

#btn-flip {
    padding: 10px 20px;
    font-size: 18px;
    border: none;
    background-color: #ffffff;
    color: #000000;
    border-radius: 5px;
    cursor: pointer;
    transition: background-color 0.3s ease;
}

#btn-flip:hover {
    background-color: #e0e0e0;
}
